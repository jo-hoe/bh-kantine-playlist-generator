# bh-playlist-generator

![Version: 3.0.0](https://img.shields.io/badge/Version-3.0.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 1.4.0](https://img.shields.io/badge/AppVersion-1.4.0-informational?style=flat-square)

A Helm chart for BH Playlist Generator

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| cronjob | object | `{"activeDeadlineSeconds":3600,"failedJobsHistoryLimit":1,"image":{"pullPolicy":"IfNotPresent","repository":"ghcr.io/jo-hoe/bh-kantine-playlist-generator"},"restartPolicy":"OnFailure","successfulJobsHistoryLimit":3,"timeZone":""}` | NOTE: Per-job scheduling is configured via jobs[].schedule. If omitted, defaults to "0 4 * * *". |
| cronjob.timeZone | string | `""` | If not set, uses cluster default (UTC). Can be overridden per job via jobs[].timeZone. |
| jobs | list | `[{"location":"kantine","maxTrackNumberPerArtist":"3","name":"default","playlistName":"Kantine Am Berghain: Next Up","schedule":"0 4 * * *","timeZone":""}]` | Jobs configuration: define one or more jobs with independent schedule and settings |
| jobs[0].location | string | `"kantine"` | Location/venue to crawl for events ('kantine' or 'klub') |
| jobs[0].maxTrackNumberPerArtist | string | `"3"` | Maximum number of tracks to add per artist to avoid playlist domination |
| jobs[0].playlistName | string | `"Kantine Am Berghain: Next Up"` | Name of the Spotify playlist to update |
| jobs[0].schedule | string | `"0 4 * * *"` | Cron schedule for this job. If omitted, defaults to "0 4 * * *" |
| jobs[0].timeZone | string | `""` | Optional timezone for this job's schedule evaluation. Example: "Europe/Berlin". If omitted, falls back to cronjob.timeZone or cluster default (UTC). |
| labels.app | string | `"bh-playlist-generator"` |  |
| podLabels | object | `{}` | Additional labels to add to the cronjob pods (optional) podLabels:   environment: production   team: platform |
| secret.data.spotifyClientId | string | `"your-spotify-client-id"` | Spotify application client ID for API authentication. NEVER commit real values to git! |
| secret.data.spotifyClientSecret | string | `"your-spotify-client-secret"` | Spotify application client secret for API authentication. NEVER commit real values to git! |
| secret.data.spotifyRedirectUri | string | `"your-spotify-redirect-uri"` | Spotify OAuth redirect URI for authentication flow. Example: http://localhost:9090/callback |
| secret.name | string | `"spotify-credentials"` |  |
| serviceAccount | object | `{"create":true,"name":"spotify-playlist-sa"}` | ServiceAccount used by the cronjob pods. It is granted RBAC to read and patch the token Secret so the pod can persist refreshed/rotated tokens. |
| tokenSecret | object | `{"create":true,"key":"token_cache.json","mountPath":"/app/secrets/token","name":"spotify-token"}` | Kubernetes Secret that stores the Spotify OAuth token (as JSON). Seed/re-seed it with: make seed-token  (or scripts/seed-token.py). See doc/token-seeding.md. |
| tokenSecret.create | bool | `true` | Create an empty placeholder Secret so the pod can patch it on first write-back. |
| tokenSecret.mountPath | string | `"/app/secrets/token"` | Path where the token Secret is mounted read-only inside the container. |
| tokenSecret.name | string | `"spotify-token"` | Name of the token Secret. |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
